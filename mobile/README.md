# ApplyFlow mobile

Bare React Native 0.86 (no Expo). Metro port **8088**. Bundle ID `com.theofanis.applyflow`.

Run instructions are in the repo root `README.md`. Always open `ios/ApplyFlow.xcworkspace`, never the `.xcodeproj`.
